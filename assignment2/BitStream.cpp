#include <string>
#include <iostream>
#include <fstream>

BitStream::BitStream(string filename) {
// read file
}

byte BitStream::readBit(
	return 'c';
)

byte[] BitStream::readNBits(
	return new byte[1];
}
